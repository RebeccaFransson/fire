

export type SavingsChange = {
  index: number;
  age: number;
  monthlySavings: number;
};

type Props = {
  savingChanges: SavingsChange[];
};

function SavingChanges({ savingChanges }: Props) {
    return (
        
    );
}